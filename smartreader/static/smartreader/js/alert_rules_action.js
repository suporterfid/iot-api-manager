document.getElementById('add-action').addEventListener('click', function () {
    // Add logic to dynamically add a new action form
});

document.querySelectorAll('.remove-action').forEach(button => {
    button.addEventListener('click', function () {
        // Remove the corresponding action form
    });
});
